apiVersion: maistra.io/v2
kind: ServiceMeshControlPlane
metadata:
  name: <cluster_name>-mesh
  namespace: <cluster_name>-system
spec:
  version: v2.1
  runtime:
    defaults:
      container:
        imagePullPolicy: Always
  gateways:
    additionalEgress: {}
    additionalIngress: {}
  security:
    trust:
      domain: <cluster_name>.local
